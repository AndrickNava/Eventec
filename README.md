# Eventec

La autenticación y la gestión de usuarios funcionan actualmente con almacenamiento temporal en memoria. El usuario inicial es `admin` con contraseña `admin123`. Los datos se pierden cuando se reinicia el proceso o se crea una nueva instancia de Vercel.

El servidor local se inicia con `python local_server.py`.